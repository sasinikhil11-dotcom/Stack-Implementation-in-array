# Stack-Implementation-in-array
A stack is a linear data structure that follows the LIFO (Last In, First Out) principle. This means the last element inserted into the stack is the first one to be removed.  This project demonstrates a simple stack implementation in C using an array, along with basic operations such as insertion, deletion, and display.
